zuul:
  sensitive-headers: null
  routes:
    game-service:
      path: /game-service/**
      service-id: durain-games
    user-service:
      path: /user-service/**
      service-id: durain-user  
management:
  endpoints:
    web:
      exposure:
        include: '*'